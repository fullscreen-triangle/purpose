/target
Cargo.lock.bak
*.swp

# purpose: derived caches. lens.toml is a decision and stays versioned.
.purpose/index.json
.purpose/ckg.json
.purpose/factory/

# profile-web: per-profile uploaded sources and trained models (runtime state).
.purpose-profiles/
